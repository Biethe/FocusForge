# scripts/

Helper scripts for setup, model fetching, and benchmarks.

- `get_models.sh` (coming in Phase 2/5): downloads the MediaPipe FaceLandmarker `.task`
  file and the SmolLM2 GGUF to `models/` (which is gitignored — weights are never committed).
